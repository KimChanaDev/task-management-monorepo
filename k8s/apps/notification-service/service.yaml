apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: task-platform-apps
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - name: http
      port: 4004
      targetPort: 4004
