apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pulsar-pvc
  namespace: task-platform-infra
  labels:
    app: pulsar
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
