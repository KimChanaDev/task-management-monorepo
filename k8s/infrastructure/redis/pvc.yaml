# Note: StorageClass is automatically updated during CI/CD deployment
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-pvc
  namespace: task-platform-infra
  labels:
    app: redis
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # Auto-replaced by CI/CD for cloud deployment
