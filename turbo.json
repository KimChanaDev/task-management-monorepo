{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"]
    },
    "lint": {
      "dependsOn": ["^lint"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "dependsOn": ["generate-ts-proto", "prisma:generate", "^build"],
      "cache": false,
      "persistent": true
    },
    "prisma:generate": {
      "dependsOn": ["^prisma:generate"]
    },
    "generate-ts-proto": {
      "dependsOn": ["^generate-ts-proto"]
    }
  }
}
